# Apps Script project ID to observe

## Overview

This Apps Script project is currently only a placeholder/scaffold.

Despite the project name, the checked-in source does **not** yet implement Apps Script project observation, project-ID monitoring, API calls, persistence, or a user interface. The only JavaScript function in `gas/コード.js` is an empty `myFunction()` stub.

## Current state

```javascript
function myFunction() {
  
}
```

The repository therefore documents this project as an unimplemented starting point rather than attributing behavior that is not present in the source.

## Files

- `gas/コード.js` — empty `myFunction()` entry point.
- `gas/appsscript.json` — Apps Script manifest.

Any future observation behavior should be documented here when it is actually added to the Apps Script source.